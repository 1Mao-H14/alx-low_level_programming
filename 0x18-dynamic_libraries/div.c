#include "main.h"
#include <stdio.h>
/**
 * div - Performs division of two numbers.
 * @a: The dividend.
 * @b: The divisor.
 *
 * Return: The result of dividing @a by @b.
 */
int div(int a, int b)
{
return (a / b);
}
