#include "main.h"
#include <stdio.h>
/**
 * mod - Calculates the modulus of two numbers.
 * @a: The dividend.
 * @b: The divisor.
 *
 * Return: The remainder of dividing @a by @b.
 */
int mod(int a, int b)
{
return (a % b);
}
