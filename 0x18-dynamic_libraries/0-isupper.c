#include "main.h"
/**
 * _isupper - A FUNCTION THATS uppercase letters
 * @c: INPUT char to check
 *
 * Return: 0 or 1
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
return (0);
}
