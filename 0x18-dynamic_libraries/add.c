#include "main.h"
#include <stdio.h>
/**
 *add - function thats add two numbers
 *@a: the first number
 *@b: the second number
 *Return: the sum of @a and @b
 **/
int add(int a, int b)
{
return (a + b);
}
